services:
  newsletter_api:
    networks:
      - proxy-network

  # listmonk app
  listmonk:
    networks:
      - proxy-network
      - proxy-network

networks:

  proxy-network: 
    name: postgres-network
    external: true

  postgres-network: 
    name: postgres-network
    external: true